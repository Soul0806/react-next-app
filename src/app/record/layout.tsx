const RecordLayout = ({ children }: { children: React.ReactNode }) => {
    return (  
        <div>
            <div>RecordLayout</div>
            {children}
        </div>
    );
}

export default RecordLayout;